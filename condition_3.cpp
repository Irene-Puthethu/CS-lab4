// Q.3 Write a C++ program to find out whether a number is positive, negative or zero.


// Include library
  #include <iostream> 
  using namespace std ; 

// Include main function
  int main() {
 
// Declare variables
  	int num;

// Ask the terminal for a number
	cout << "Write a number." << endl;
        cin >> num;

// Write condition to check whether a number is postive,negative or zero.
	{
	 if (num > 0) 
// To do
	 cout << num <<" is a positive number." << endl;
	
	 else if (num < 0)
// To do
	 cout << num <<" is a negative number." << endl;

	 else 
// To do
	 cout << num <<" is Zero." << endl;
	};
 

             return 0;
                }
